/// @description Movimentação do Asteroide
// Inicializando a movimentação do Asteroide

randomize();

rotacao = random_range(1,2);
speed = random_range(0.4,1);
direction = irandom_range(0,359);

vida = 3;
